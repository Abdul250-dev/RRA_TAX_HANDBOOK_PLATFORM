# Architecture

This repository is organized as a monorepo with:

- `apps/backend`: Spring Boot API
- `apps/public-frontend`: public Next.js website
- `apps/admin-frontend`: internal Next.js admin dashboard
- `infra`: deployment and local environment assets

Legacy backend notes remain in `apps/backend/docs`.
